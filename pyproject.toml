[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "handyrec"
authors = [{name = "Weipeng Zhang", email = "wp.zhang@hotmail.com"}]
dynamic = ["version", "description"]
