FeatureGroups:
    item_seq_feat_group:
        type: "FeatureGroup"
        name: "item_seq"

        SparseSeqFeatures:
            hist_movie:
                unit:
                    movie_id:
                        embedding_dim: 8
                seq_len: 2
